---
title: Writing
permalink: writing/index.html
---
I post new articles as I write them. I focus on quality rather than frequency. These are the archives of everything I've written since 2014.

<ol class="ml-0 list-none">
{%- for post in collections.allPosts | reverse -%}
    <li class="flex flex-col mt-8">
        <span class="font-sans text-base text-gray-700">{{ post.date | readableDate }}</span>
        <h2 class="mb-0 text-lg md:text-xl">
            <a class="font-medium font-serif-title" href="{{ post.url }}">{{ post.data.title }}</a>
        </h2>
    </li>
{%- endfor -%}
</ol>